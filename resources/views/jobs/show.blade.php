@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-4xl px-6 py-12"><a href="{{ route('jobs.index') }}" class="text-sm font-semibold text-brand">← Back to jobs</a><div class="mt-8 rounded-2xl border border-slate-200 bg-white p-8 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-6"><div><p class="font-semibold text-brand">{{ $job->company->name }}</p><h1 class="mt-2 text-3xl font-bold">{{ $job->title }}</h1><p class="mt-3 text-slate-500">{{ $job->location }} · {{ $job->employment_type }} · {{ $job->category?->name ?? 'General' }}</p></div><a href="{{ route('applications.create', $job) }}" class="rounded-lg bg-brand px-5 py-3 font-bold text-white hover:bg-brand-dark">Apply now</a></div><div class="my-8 border-t border-slate-100"></div><h2 class="text-xl font-bold">About the role</h2><div class="mt-4 whitespace-pre-line leading-7 text-slate-600">{{ $job->description }}</div>@if($job->salary_min)<p class="mt-8 font-semibold text-slate-800">Salary: ${{ number_format($job->salary_min) }} - ${{ number_format($job->salary_max) }}</p>@endif</div></div>
@endsection
