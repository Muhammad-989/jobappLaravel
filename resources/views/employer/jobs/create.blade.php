@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-3xl px-6 py-12"><a href="{{ route('employer.dashboard') }}" class="text-sm font-semibold text-brand">← Dashboard</a><h1 class="mt-8 text-3xl font-bold">Post a new job</h1><form method="POST" action="{{ route('employer.jobs.store') }}" class="mt-8 grid gap-5 rounded-xl border border-slate-200 bg-white p-6">@csrf
<label class="grid gap-2 text-sm font-semibold">Job title<input name="title" value="{{ old('title') }}" required class="rounded-lg border-slate-300 px-4 py-3"></label><label class="grid gap-2 text-sm font-semibold">Location<input name="location" value="{{ old('location') }}" placeholder="City or Remote" required class="rounded-lg border-slate-300 px-4 py-3"></label><label class="grid gap-2 text-sm font-semibold">Category<select name="category_id" class="rounded-lg border-slate-300 px-4 py-3"><option value="">General</option>@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select></label><label class="grid gap-2 text-sm font-semibold">Employment type<select name="employment_type" class="rounded-lg border-slate-300 px-4 py-3">@foreach(['Full-time','Part-time','Contract','Remote'] as $type)<option>{{ $type }}</option>@endforeach</select></label><div class="grid gap-4 sm:grid-cols-2"><input name="salary_min" type="number" placeholder="Minimum salary" class="rounded-lg border-slate-300 px-4 py-3"><input name="salary_max" type="number" placeholder="Maximum salary" class="rounded-lg border-slate-300 px-4 py-3"></div><label class="grid gap-2 text-sm font-semibold">Description<textarea name="description" rows="10" required class="rounded-lg border-slate-300 px-4 py-3">{{ old('description') }}</textarea></label>@if($errors->any())<div class="text-sm text-red-700">{{ $errors->first() }}</div>@endif<button class="rounded-lg bg-brand px-5 py-3 font-bold text-white hover:bg-brand-dark">Publish job</button></form></div>
@endsection
