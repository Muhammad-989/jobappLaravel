@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-5xl px-6 py-12"><a href="{{ route('employer.dashboard') }}" class="text-sm font-semibold text-brand">← Dashboard</a><h1 class="mt-8 text-3xl font-bold">Applications for {{ $job->title }}</h1><div class="mt-8 grid gap-4">@forelse($applications as $application)<article class="rounded-xl border border-slate-200 bg-white p-6"><div class="flex flex-wrap items-start justify-between gap-4"><div><h2 class="text-lg font-bold">{{ $application->user->name }}</h2><p class="text-sm text-slate-500">{{ $application->user->email }} · Applied {{ $application->created_at->diffForHumans() }}</p></div><form method="POST" action="{{ route('employer.applications.update', $application) }}" class="flex gap-2">@csrf @method('PATCH')<select name="status" class="rounded-lg border-slate-300 px-3 py-2 text-sm">@foreach(['submitted','reviewing','interview','accepted','rejected'] as $status)<option value="{{ $status }}" @selected($application->status === $status)>{{ ucfirst($status) }}</option>@endforeach</select><button class="rounded-lg bg-brand px-3 py-2 text-sm font-semibold text-white">Update</button></form></div><p class="mt-5 whitespace-pre-line text-slate-600">{{ $application->cover_letter }}</p></article>@empty<div class="rounded-xl border border-dashed border-slate-300 p-10 text-center text-slate-500">No applications yet.</div>@endforelse</div></div>
@endsection
