@extends('layouts.app')
@section('content')
<div class="mx-auto max-w-6xl px-6 py-12"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="font-semibold text-brand">Employer workspace</p><h1 class="mt-2 text-3xl font-bold">Manage your company</h1></div>@if($company)<a href="{{ route('employer.jobs.create') }}" class="rounded-lg bg-brand px-5 py-3 font-bold text-white hover:bg-brand-dark">Post a job</a>@endif</div>
@if(!$company)<div class="mt-8 max-w-xl rounded-xl border border-slate-200 bg-white p-6"><h2 class="text-xl font-bold">Set up your company</h2><p class="mt-2 text-slate-500">Add these details once before publishing your first job.</p>
@if($errors->any())<div class="mt-4 rounded-lg bg-red-50 px-4 py-3 text-sm text-red-800"><p class="font-semibold">Please fix the following:</p><ul class="mt-1 list-disc pl-5">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
<form method="POST" action="{{ route('employer.company.store') }}" class="mt-6 grid gap-4">@csrf<input name="name" value="{{ old('name') }}" placeholder="Company name" required class="rounded-lg border-slate-300 px-4 py-3"><input name="location" value="{{ old('location') }}" placeholder="Location" class="rounded-lg border-slate-300 px-4 py-3"><input name="website" value="{{ old('website') }}" placeholder="Website (https://...)" type="url" class="rounded-lg border-slate-300 px-4 py-3"><p class="-mt-2 text-xs text-slate-500">Include https://, for example https://example.com</p><textarea name="description" placeholder="What does your company do?" rows="4" class="rounded-lg border-slate-300 px-4 py-3">{{ old('description') }}</textarea><button class="rounded-lg bg-brand px-5 py-3 font-bold text-white">Save company</button></form></div>
@else<div class="mt-8 grid gap-8 lg:grid-cols-[280px_1fr]"><aside class="rounded-xl border border-slate-200 bg-white p-6"><h2 class="text-xl font-bold">{{ $company->name }}</h2><p class="mt-2 text-slate-500">{{ $company->location }}</p><p class="mt-4 text-sm text-slate-600">{{ $company->description }}</p></aside><section><h2 class="text-xl font-bold">Your job postings</h2><div class="mt-4 grid gap-3">@forelse($jobs as $job)<div class="flex items-center justify-between rounded-xl border border-slate-200 bg-white p-5"><div><h3 class="font-bold">{{ $job->title }}</h3><p class="mt-1 text-sm text-slate-500">{{ $job->location }} · <a href="{{ route('employer.jobs.applications', $job) }}" class="text-brand hover:underline">{{ $job->applications_count }} applications</a></p></div><span class="rounded-full bg-green-50 px-3 py-1 text-xs font-semibold text-green-700">{{ ucfirst($job->status) }}</span></div>@empty<p class="rounded-xl border border-dashed border-slate-300 p-8 text-slate-500">You have not posted a job yet.</p>@endforelse</div></section></div>@endif</div>
@endsection
